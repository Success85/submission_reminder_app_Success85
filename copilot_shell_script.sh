#!/bin/bash

#Prompt user(student) to enter the name of the assignment to be checked for submission

#Write the assignment name in submission_reminder_name/config/config.env

#Replace the current assignment name with the new name entered by the user

#Run the startup.sh to check the non-submission status of the new assignment that is saved in config/config.env

#Test and push
